# 网站部署验证指南

本指南将帮助您验证网站是否成功部署到GitHub Pages。

## 一、检查GitHub Pages部署状态

1. 打开GitHub仓库页面：https://github.com/YwaitMe/xiaoxuan
2. 点击顶部导航栏中的**Settings**（设置）选项
3. 在左侧菜单中点击**Pages**（页面）选项
4. 查看"Build and deployment"（构建和部署）部分：
   - 如果看到绿色的对勾图标，表示部署成功
   - 部署状态信息会显示最后一次部署的时间和结果

## 二、访问部署的网站

如果GitHub Pages部署成功，您可以通过以下URL访问网站：

```
https://ywaitme.github.io/xiaoxuan/
```

请将该URL复制到浏览器中访问，检查网站是否可以正常打开和使用。

## 三、常见部署问题排查

如果遇到部署问题，可以尝试以下排查步骤：

### 1. 检查部署源设置
- 确保GitHub Pages的部署源设置为`gh-pages`分支
- 确保部署文件夹设置为根目录`/`

### 2. 检查gh-pages分支内容
- 确保`gh-pages`分支包含网站的所有必要文件（HTML、CSS、JavaScript等）
- 确保`index.html`文件存在于分支的根目录

### 3. 检查文件路径
- 确保网站内部的链接和资源引用使用相对路径
- 避免使用绝对路径引用本地文件

### 4. 等待部署完成
- 首次部署或更新代码后，GitHub Pages可能需要几分钟时间来构建和部署网站
- 请耐心等待后再尝试访问

### 5. 清除浏览器缓存
- 浏览器可能会缓存旧版本的网站
- 尝试使用Ctrl+Shift+R（Windows）或Cmd+Shift+R（Mac）强制刷新页面

## 四、验证网站功能

成功访问网站后，建议验证以下功能是否正常工作：

1. **首页显示**：检查页面是否完整加载，包括所有文字、图片和布局
2. **功能测试**：
   - 尝试进行长按测试，检查压力数据是否正常记录和显示
   - 切换不同的主题（明亮/暗黑）
   - 尝试导出测试数据
3. **响应式设计**：在不同设备或浏览器窗口大小下检查网站布局是否正常

## 五、更新部署

如果您需要更新网站内容，可以按照以下步骤操作：

1. 在本地修改代码文件
2. 提交修改到git仓库：
   ```bash
   git add .
   git commit -m "更新网站内容"
   ```
3. 推送到GitHub的gh-pages分支：
   ```bash
   git push origin gh-pages
   ```
4. 等待GitHub Pages自动部署更新后的内容
5. 按照本指南的步骤验证更新是否成功

如果您遇到任何部署问题，请参考GitHub Pages的官方文档或查看仓库的Issues部分寻求帮助。